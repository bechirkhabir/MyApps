This project :
1 Reads 3d mesh file in MEDIT format
2 Runs through all vertices, edges and mesh faces and computes different geometry descriptors that can be used to asses the geometry represented by the mesh
3 Geometry descriptos combined with the mesh setting will be used to build a ML model 
